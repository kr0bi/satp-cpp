# SATP-C++ Handoff (Codex Chat Context)

Generated on: **2026-03-04 01:08:02 CET**

This file is a repository-local handoff for continuing work in another Codex session.
It summarizes the key decisions, implementation state, workflow, and open directions from the current chat.

---

## 1) Project Goal (Current Scope)

Master thesis project on sketching algorithms (distinct counting first), with:

- **C++ core**: algorithms + benchmark/evaluation framework.
- **Python tooling**: dataset generation + orchestration + notebooks.
- **LaTeX thesis**: methodology, implementation, experiments, and discussion.

Primary implemented family:

1. NaiveCounting (exact baseline)
2. Probabilistic Counting
3. LogLog
4. HyperLogLog
5. HyperLogLog++

Planned/next (not yet fully integrated): Count-Min Sketch, Bloom Filter, Ring Bloom.

---

## 2) Dataset Evolution and Current Format

### 2.1 Historical evolution (important context)

- Initial text datasets and C++ generation utilities were removed/deprecated.
- JSON dataset phase existed but was later dropped for size/performance reasons.
- Current standard is a **binary compressed format** read by C++ and generated by Python.

### 2.2 Current binary dataset format (v2)

Key points:

- File is partitioned; each partition is compressed independently (`zlib`).
- C++ loads **one partition at a time** in RAM.
- Includes truth information for streaming prefix analysis \(F_0(t)\).

Header:

- `MAGIC=SATPDBN2`
- `VERSION=2`
- global `n`, `d`, `p`, `seed`

Partition table entry:

- `ENTRY_SIZE = 60` bytes from struct layout `<QQQQQQIII>`
- 6 × `Q` (uint64) + 3 × `I` (uint32) = 48 + 12 = 60
- fields: offsets/sizes for values payload and truth payload, local `n,d`, encodings.

Encodings:

- values: `ENCODING_ZLIB_U32_LE`
- truth bits: `ENCODING_ZLIB_BITSET_LE`

Truth semantics:

- bit `b_t=1` iff element at position `t` is first occurrence in that partition
- exact prefix cardinality reconstructed as:
  \[
  F_0(t)=\sum_{i=1}^{t} b_i
  \]

### 2.3 Naming

Dataset naming convention in use:

- `dataset_n_{n}_d_{d}_p_{p}_s_{seed}.bin`

---

## 3) Experimental Modes in C++ Framework

Implemented in `EvaluationFramework` / CLI:

1. **normal (oneshot)**  
   one final estimate per run/partition, aggregate metrics across runs.

2. **streaming**  
   metrics at checkpoints over prefixes; truth from embedded bitset (`F_0(t)`).

3. **merge**  
   pairwise merge vs serial comparison on pairs `(0,1), (2,3), ...`.

Important behavior:

- `runs`, `sample_size`, and `seed` are derived from dataset metadata.
- Legacy manual assumptions were removed.

---

## 4) CSV Schema and Naming

Column naming was normalized (notably typo fix from `f0_heat_mean_t` to `f0_hat_mean_t`).

Shared normal/streaming schema:

`algorithm,params,mode,runs,sample_size,number_of_elements_processed,f0,seed,f0_mean_t,f0_hat_mean_t,variance,stddev,rse_theoretical,rse_observed,bias,absolute_bias,relative_bias,mean_relative_error,rmse,mae`

Merge schema:

`algorithm,params,mode,pairs,sample_size,pair_index,seed,estimate_merge,estimate_serial,delta_merge_serial_abs,delta_merge_serial_rel`

Result paths are namespace-based:

- `results/<namespace>/<mode>/<algorithm>/<params>/results_<mode>.csv`

Namespaces currently used:

- `legacy`
- `prefix_constant_rho`

---

## 5) Python Tooling (Current)

Main scripts:

- `scripts/generate_partitioned_dataset_bin.py` (uniform/shuffled legacy flow)
- `scripts/generate_prefix_constant_rho_dataset_bin.py` (special protocol for Type A/B analyses)
- `scripts/orchestrate_benchmarks.py` (dataset matrix + benchmark runs)
- `scripts/generate_prefix_constant_rho_set.sh` (batch generation helper)

Tests:

- `scripts/tests/test_generate_dataset.py`
- `scripts/tests/test_generate_prefix_constant_rho_dataset.py`

All python tests currently pass.

---

## 6) Type A / Type B Analysis Protocol (Advisor-Driven)

Advisor requested clearer graphs and interpretation.

### Type A

- x-axis: **real cardinality** on prefix, \(F_0(t)\)
- y-axis: estimated cardinality, \(\hat F_0(t)\)
- compare algorithms, include uncertainty (stddev/intervals)

### Type B

- x-axis: average repetitions on prefix:
  \[
  \rho_t = \frac{t}{F_0(t)}
  \]
- y-axis: \(\hat F_0\) at selected target cardinalities \(F_0^\*\)

### Prefix-constant-rho generation rationale

For controlled behavior:

- enforce `n % d == 0`
- \(\rho = n/d\)
- one new distinct every \(\rho\) items (block protocol)
- then \(F_0(j\rho)=j\)

This was introduced specifically to decouple effects and make Type A/Type B interpretability stronger.

---

## 7) Merge Interpretation (Important)

Observed and expected:

- For LogLog/HLL/HLL++ with same params + same hash behavior, merge is equivalent to serial processing at sketch-state level.
- Therefore merge-vs-serial deltas are expected near/at zero in current setup.

Advisor suggested as further study:

- degenerate/heterogeneous merge cases (different parameters/hash settings).

---

## 8) Thesis Integration State

Chapters renamed/organized in Italian structure:

1. Introduzione
2. Background
3. Analisi stato dell’arte
4. Implementazione
5. Risultati sperimentali
6. Conclusioni

Chapter 4/5 heavily aligned with actual code/CSV/notebooks.
Dataset format, framework modes, metrics, and Type A/B graph methodology are documented in thesis.

Notebooks used by thesis figures (current active set):

- `16_ch5_typeA_prefix_constant_rho.ipynb`
- `17_ch5_typeA_prefix_constant_rho_no_pc.ipynb`
- `18_ch5_typeA_prefix_constant_rho_hll_hllpp.ipynb`
- `19_ch5_typeB_prefix_constant_rho.ipynb`

Other notebooks exist but are considered older/auxiliary.

---

## 9) Recent Hygiene + Tooling Work Already Done

Completed in this chat:

- `.gitignore` improved for Python/Jupyter artifacts and LaTeX busy synctex.
- `CMakeLists.txt` simplified by removing duplicated explicit file lists when already using `GLOB_RECURSE`.
- `README.md` aligned to real results path structure (namespace + mode).
- local cleanup of `.DS_Store` and `__pycache__` artifacts.

Build/tests status after tooling cleanup:

- CMake build: OK
- CTest: 25/25 passed
- Python script tests: passed

---

## 10) Explicit User Constraints / Decisions

User requested:

- Keep CSV files as they are (**do not touch CSV data now**).
- Do hygiene + tooling only.
- Do **not** freeze/remove legacy at this step.
- Postpone code refactor.
- Do not add a quality-gate pipeline now.

---

## 11) Open Work (High Level)

Technical backlog currently emphasized:

1. Implement Count-Min Sketch.
2. Implement Bloom Filter.
3. Implement Ring Bloom Filter.
4. Extend merge experiments to heterogeneous scenarios and/or k-way topologies.
5. Add empirical performance profiling (update/query/merge time + memory footprint).
6. Optional hash-robustness campaign (different hash functions/seedable hash).

---

## 12) Useful Commands (Quick Resume)

Build/test:

```bash
cmake -S . -B build
cmake --build build -j 8
ctest --test-dir build --output-on-failure
python3 -m unittest discover -s scripts/tests -v
```

Orchestration:

```bash
python3 scripts/orchestrate_benchmarks.py --skip-generate --skip-oneshot
```

Dataset generation (prefix-constant-rho):

```bash
python3 scripts/generate_prefix_constant_rho_dataset_bin.py \
  --output-dir datasets/prefix_constant_rho \
  --n 10000000 --d 1000000 --p 50 --seed 21041998
```

---

## 13) Notes for Next Codex Session

If continuing here, first check:

1. `git status`
2. consistency between thesis references and figure files
3. whether next step is:
   - graph refresh from existing CSVs, or
   - new algorithm implementation (CMS/Bloom), or
   - controlled heterogeneous merge experiments.

