\chapter{Analisi dello stato dell'arte}
\label{chp:stato-arte}

Nel Capitolo~\ref{chp:background} sono state introdotte le nozioni teoriche che
servono per poter comprendere il problema del conteggio degli elementi distinti nei flussi di dati.
Ora si vuole analizzare la parte algoritmica relativa al problema. L'obiettivo è ricostruire lo
sviluppo che porta dal conteggio probabilistico originario di Flajolet e Martin
fino a HyperLogLog++, chiarendo per ogni metodo come è fatto lo stato, come
avviene l'aggiornamento, quale stima produce, quale costo richiede e in che modo
si presta al merge di sketch. Si esaminano nell'ordine Probabilistic Counting,
PCSA, LogLog, HyperLogLog e HyperLogLog++. Per ciascun algoritmo si descrivono
stato, aggiornamento, stima, complessità e proprietà di merge. Infine si
richiamano Bloom filter e Count-Min Sketch come esempi di sketch usati per
obiettivi diversi dal solo conteggio dei distinti.

\section{Obiettivo del problema e vincoli teorici}
Il problema del conteggio degli elementi distinti consiste nello stimare
\[
F_0 = |\{a\in\mathcal{U} : f(a)>0\}|
\]
ossia la cardinalità del supporto del vettore delle frequenze. Nel quadro dei
\emph{frequency moments}, $F_0$ rappresenta il primo problema di stima
con memoria sublineare sui flussi di dati
\cite{Alon_Matias_Szegedy_2002,BarYossef_Jayram_Kumar_Sivakumar_Trevisan_2002}

In un modello \textit{insertion-only}, un algoritmo deve processare ogni elemento in
uno o pochi passaggi, con tempo di aggiornamento molto basso e stato ridotto. Il
calcolo esatto richiede in generale memoria lineare nel numero di distinti
osservati e quindi non può scalare quando il flusso cresce senza un limite
prefissato. Per questo motivo la letteratura ricorre a sketch randomizzati con
garanzie probabilistiche $(\varepsilon,\delta)$, già formalizzate nel
Capitolo~\ref{chp:background}

Dal lato teorico esistono algoritmi ottimali che raggiungono spazio dell'ordine
$O((\varepsilon^{-2} + \log n)\operatorname{polylog} n)$, mostrando che la
dipendenza da $\varepsilon^{-2}$ è strutturale e non una conseguenza
implementativa \cite{Kane_Nelson_Woodruff_2010}. Questo, però è un risultato
moderno, utile come termine di paragone: gli algoritmi studiati nel seguito
nascono prima di questi risultati ottimali e vanno letti come tappe storiche
che migliorano progressivamente il compromesso tra spazio e accuratezza.

Nel seguito gli algoritmi vengono confrontati secondo sette criteri:
\begin{itemize}
    \item forma dello stato dello sketch;
    \item regola di aggiornamento;
    \item forma dello stimatore;
    \item errore tipico o comportamento dell'errore;
    \item costo in spazio;
    \item tempo di aggiornamento e di interrogazione;
    \item proprietà di merge di sketch in scenari distribuiti.
\end{itemize}

\section{Sviluppo storico}
Lo sviluppo storico degli algoritmi per il conteggio dei distinti segue una percorso
piuttosto lineare. Si parte da un vettore di bit, chiamato bitmap, che tiene conto della
frequenza di eventi poco frequenti, per poi passare
a una mediazione stocastica su più bitmap. A seguire una famiglia di registri
che usa una funzione di massimo sul rango dei registri, quindi a una stima armonica con correzioni di intervallo,
e infine a una variante pratica che aggiunge correzione del bias e
rappresentazione sparsa.

\begin{definition}[Probabilistic Counting]
  Sia $h:\mathcal{U}\rightarrow\{0,1\}^{L}$ una funzione di hash uniforme. Nella
  versione elementare del metodo si mantiene una sola bitmap
  $B=(b_1,\dots,b_L)\in\{0,1\}^{L}$ inizializzata a zero.

  Per ogni valore hash $y$, si considera la quantità $\rho(y)$, che rappresenta
  la posizione della bitmap da porre a uno. In altre parole, $\rho(y)$ misura
  quanti zeri consecutivi compaiono a partire dal bit meno significativo di $y$,
  più uno. Nella convenzione usata qui si pone
  \[
  \rho(y)=\min\{1+\nu_2(y),L\}
  \]
  dove $\nu_2(y)$ è il numero di zeri consecutivi a partire dal bit meno
  significativo.

  Per ogni elemento $x$ del flusso si aggiorna quindi la bitmap ponendo
  \[
  b_{\rho(h(x))}\leftarrow 1
  \]

  Indicando con
  \[
  R(B)=\min\bigl(\{r\geq 1 : b_r=0\}\cup\{L+1\}\bigr)
  \]
  la posizione del primo zero nella bitmap, con la convenzione che se la bitmap è
  interamente a uno si ponga $R(B)=L+1$, la stima assume la forma
  \[
  \hat{F}_0 \approx \frac{2^{R(B)}}{\phi}
  \]
  dove $\phi$ è una costante di calibrazione empirica
  \cite{Flajolet_Martin_1985}
  \end{definition}

La struttura è estremamente compatta e mostra già il principio fondamentale del
conteggio probabilistico, ma una sola bitmap produce una varianza elevata. Per
questa ragione la versione elementare è importante soprattutto come punto di
partenza concettuale e come riferimento storico.

\begin{algorithm}[htbp]
    \caption{Probabilistic Counting}
    \label{alg:pc}
    \begin{algorithmic}
        \STATE Choose a bitmap $B[1],\dots,B[L]$ and initialize it to $0$
        \STATE Choose a uniform hash function $h:\mathcal{U}\rightarrow\{0,1\}^{L}$
        \FOR{each element $x$ in the stream}
            \STATE $y \leftarrow h(x)$
            \STATE $r \leftarrow \rho(y)$
            \STATE $B[r]\leftarrow 1$
        \ENDFOR
        \STATE $R \leftarrow \textsc{FirstRightmostZero}(B)$
        \STATE \textbf{return} $\hat{F}_0 \leftarrow 2^R/\phi$
    \end{algorithmic}
\end{algorithm}

\paragraph{Esempio}
Supponiamo di usare una bitmap di lunghezza $L=5$. Consideriamo la sequenza di
valori hash
$00101_2$, $00010_2$, $11100_2$, $01000_2$

Nel primo caso $\rho=1$, nel secondo $\rho=2$, nel
terzo $\rho=3$ e nel quarto $\rho=4$. Dopo questi
quattro aggiornamenti la bitmap vale
\[
B=[11110]
\]
quindi il primo zero si trova in posizione $R(B)=5$ e la stima è circa
$2^5/\phi$. L'esempio è volutamente minimo, ma mostra bene la logica del
metodo: gli eventi pi\`u rari osservati sull'hash vengono trasformati in una scala di
cardinalità.

\paragraph{Errore e limiti}
La versione a bitmap singola è soggetta molto alla varianza. Il vantaggio è la semplicità,
ma l'errore resta troppo elevato per un uso pratico effettivo. È proprio da
questa debolezza che nasce il passo successivo, cioè PCSA.

\paragraph{Complessità}
Lo spazio è $\Theta(L)\,\text{bit}$, mentre la complessita in tempo è $\Theta(1)$ per elemento. 
Il tempo di query richiede
$\Theta(L)$ nel caso diretto, oppure tempo costante se si mantengono informazioni ausiliari
sulla posizione del primo zero.

\subsection{Probabilistic Counting with Stochastic Averaging}
Nello stesso lavoro del 1985, Flajolet e Martin introducono
\emph{Probabilistic Counting with Stochastic Averaging} (\textbf{PCSA})
\cite{Flajolet_Martin_1985}. L'idea è non affidare più tutta la stima a una
sola bitmap, ma distribuire l'informazione su più bitmap virtuali ottenute
tramite partizione del flusso.

\begin{definition}[PCSA]
Siano $m$ il numero di bitmap e $L$ la loro lunghezza. Ogni
elemento $x$ viene trasformato in un valore hash $y=h(x)$ e da esso si
ricavano:
\[
j(x)= y \bmod m
\]
\[
t(x)= \lfloor y/m \rfloor
\]

Il valore $j(x)$ seleziona la bitmap da aggiornare, mentre
$t(x)$ viene usato per calcolare il rango $\rho(t(x))$

Per ciascuna bitmap $B[j]$ si calcola il primo zero $R_j$ e la stima finale usa
la media dei valori osservati:
\[
\bar{R}=\frac{1}{m}\sum_{j=0}^{m-1} R_j
\]
\[
\hat{F}_0 \approx \frac{m}{\phi}\cdot 2^{\bar{R}}
\]
\end{definition}

In questo modo la partizione è virtuale e dipende soltanto dalla funzione di
hash. Non vengono create più sequenze fisiche separate, ma più sottostrutture
che raccolgono informazione in modo calcolato.

Sotto l'ipotesi che il valore usato per aggiornare ciascuna bitmap sia
uniforme, la variabile $\rho(z)$ conserva coda geometrica:
\[
\Pr[\rho(z)\ge \tau]=2^{-(\tau-1)}
\]
Questa proprietà è il fondamento probabilistico del metodo. PCSA conserva la
stessa idea del conteggio probabilistico originario, ma la applica su più
bitmap virtuali, riducendo la varianza della stima attraverso la media dei
contributi osservati.

\begin{algorithm}[htbp]
    \caption{PCSA}
    \label{alg:pcsa}
    \begin{algorithmic}
        \STATE Choose $m$ bitmaps $B[0],\dots,B[m-1]$ of length $L$ and initialize them to $0$
        \STATE Choose a uniform hash function $h:\mathcal{U}\rightarrow\{0,1\}^{L}$
        \FOR{each element $x$ in the stream}
            \STATE $y \leftarrow h(x)$
            \STATE $j \leftarrow y \bmod m$
            \STATE $t \leftarrow \lfloor y/m \rfloor$
            \STATE $r \leftarrow \rho(t)$
            \STATE Set $B[j,r]\leftarrow 1$
        \ENDFOR
        \FOR{$j=0$ \TO $m-1$}
            \STATE $R_j \leftarrow \textsc{FirstZero}(B[j])$
        \ENDFOR
        \STATE $\bar{R} \leftarrow \frac{1}{m}\sum_{j=0}^{m-1} R_j$
        \STATE \textbf{return} $\hat{F}_0 \leftarrow \frac{m}{\phi}\cdot 2^{\bar{R}}$
    \end{algorithmic}
\end{algorithm}

\paragraph{Esempio}
Consideriamo $m=4$ bitmap di lunghezza $4$ e rappresentiamo ogni
bitmap come $[b_1b_2b_3b_4]$, dove $b_r=1$ indica che la posizione $r$ è stata
osservata almeno una volta.

\begin{table}[htbp]
    \centering
    \small
    \setlength{\tabcolsep}{4pt}
    \renewcommand{\arraystretch}{1.1}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|c|c|c|c|c|c|c|c|c|}
        \hline
        Passo & $x$ & $y=h(x)$ & $j=y\bmod 4$ & $t=\lfloor y/4\rfloor$ & $r=\rho(t)$ & Aggiornamento & Stato bitmap $(B_0|B_1|B_2|B_3)$ & $\hat{F}_0$ \\
        \hline
        1 & $a$ & 25 & 1 & $6\;(110_2)$ & 2 & $B[1,2]\leftarrow 1$ & $0000\;|\;0100\;|\;0000\;|\;0000$ & $\approx 10.34$ \\
        \hline
        2 & $b$ & 14 & 2 & $3\;(11_2)$ & 1 & $B[2,1]\leftarrow 1$ & $0000\;|\;0100\;|\;1000\;|\;0000$ & $\approx 12.29$ \\
        \hline
        3 & $c$ & 9 & 1 & $2\;(10_2)$ & 2 & $B[1,2]\leftarrow 1$ & $0000\;|\;0100\;|\;1000\;|\;0000$ & $\approx 12.29$ \\
        \hline
        4 & $d$ & 7 & 3 & $1\;(1_2)$ & 1 & $B[3,1]\leftarrow 1$ & $0000\;|\;0100\;|\;1000\;|\;1000$ & $\approx 14.62$ \\
        \hline
    \end{tabular}
    }
    \caption{Esempio operativo di PCSA}
    \label{tab:pcsa-esempio}
\end{table}

La tabella mostra che gli elementi vengono distribuiti tra sottostrutture
diverse. La stima finale non dipende più da una sola bitmap, ma dalla media dei
primi zeri osservati in più bitmap.

Per PCSA la costante $\phi$ viene stimata empiricamente. L'errore relativo
standard atteso è circa $0.78/\sqrt{m}$.

Gli autori riportano anche ordini di grandezza pratici: con
$m=64$ si ottiene tipicamente un errore intorno al $10\%$, mentre
$m=256$ si scende intorno al $5\%$

\paragraph{Complessità}
Lo spazio per questa struttura dati, dato $m$ ed $L$ \`e $\Theta(mL)\,\text{bit}$.

L'aggiornamento della struttura richiede un tempo $\Theta(1)$ per elemento. Mentre 
fare una query sulla struttura richiede $\Theta(mL)$ nel caso diretto, oppure $\Theta(m)$
se si mantiene informazione ausiliaria sul primo zero di ciascuna bitmap.

Il limite principale di PCSA non è più solo la varianza, ma anche il costo di
mantenere molte bitmap. Da qui nasce il passaggio a strutture a registri,
introdotto da LogLog.

\subsection{LogLog}
LogLog sostituisce la bitmap con un array di registri e applica la mediazione
stocastica in modo più efficiente \cite{Durand_Flajolet_2003}. Il prefisso
dell'hash seleziona il registro da aggiornare, mentre il suffisso determina il
rango da propagare come massimo. La stima finale usa una media geometrica normalizzata.

\begin{definition}[LogLog]
Sia $m=2^p$ il numero di registri. Per ogni elemento $x$ si calcola un valore
di hash $y=h(x)$, si usa il prefisso di $p$ bit per selezionare il registro
\(j(x)\), e il suffisso \(w(x)\) per calcolare il rango
\[
\rho(w)=
\begin{cases}
\min\{r\geq 1 : w_r=1\}, & \text{se esiste un bit a }1 \\[1mm]
|w|+1, & \text{altrimenti}
\end{cases}
\]
dove i bit del suffisso sono letti da sinistra verso destra

L'update della struttura viene fatto, quindi come:
\[
M[j(x)] \leftarrow \max\{M[j(x)],\rho(w(x))\}
\]

Indicando con
\[
A=\frac{1}{m}\sum_{j=0}^{m-1} M[j]
\]
lo stimatore LogLog ha forma
\[
\hat{F}_0 = \alpha_m \cdot m \cdot 2^{A}
\]
dove $\alpha_m$ è una costante di calibrazione (asintoticamente circa $0.397$).
\end{definition}

La relazione con $F_0$ può essere letta anche in modo esplicito: il numero
atteso di elementi che cade in ciascun registro è circa $F_0/m$, quindi i
massimi dei registri tendono a concentrarsi attorno a $\log_2(F_0/m)$.

\begin{algorithm}[htbp]
    \caption{LogLog}
    \label{alg:loglog}
    \begin{algorithmic}
        \STATE Choose the precision $p$ and set $m=2^p$
        \STATE Initialize the registers $M[0],\dots,M[m-1]\leftarrow 0$
        \FOR{each element $x$ in the stream}
            \STATE $y \leftarrow h(x)$
            \STATE $j \leftarrow \textsc{Prefix}_p(y)$
            \STATE $w \leftarrow \textsc{Suffix}_{L-p}(y)$
            \STATE $M[j] \leftarrow \max\{M[j],\rho(w)\}$
        \ENDFOR
        \STATE $A \leftarrow \frac{1}{m}\sum_{j=0}^{m-1} M[j]$
        \STATE \textbf{return} $\hat{F}_0 \leftarrow \alpha_m \cdot m \cdot 2^A$
    \end{algorithmic}
\end{algorithm}

\paragraph{Esempio}
Consideriamo un caso minimale con $p=2$, quindi $m=4$ registri, e hash su
$8$ bit. I primi due bit selezionano il registro, i restanti sei bit formano il
suffisso.

\begin{table}[htbp]
    \centering
    \small
    \setlength{\tabcolsep}{4pt}
    \renewcommand{\arraystretch}{1.1}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|c|c|c|c|c|c|c|c|c|}
        \hline
        Passo & $x$ & $y=h(x)$ & $j$ & $w$ & $\rho(w)$ & Aggiornamento & Stato registri $(M_0,M_1,M_2,M_3)$ & $\hat{F}_0$ \\
        \hline
        1 & $a$ & $10010100_2$ & $2$ & $010100_2$ & $2$ & $M[2]\leftarrow 2$ & $(0,0,2,0)$ & $\approx 1.77$ \\
        \hline
        2 & $b$ & $10111000_2$ & $2$ & $111000_2$ & $1$ & $M[2]\leftarrow 2$ & $(0,0,2,0)$ & $\approx 1.77$ \\
        \hline
        3 & $c$ & $01001100_2$ & $1$ & $001100_2$ & $3$ & $M[1]\leftarrow 3$ & $(0,3,2,0)$ & $\approx 2.97$ \\
        \hline
        4 & $d$ & $11001000_2$ & $3$ & $001000_2$ & $3$ & $M[3]\leftarrow 3$ & $(0,3,2,3)$ & $\approx 4.99$ \\
        \hline
        5 & $e$ & $10000100_2$ & $2$ & $000100_2$ & $4$ & $M[2]\leftarrow 4$ & $(0,3,4,3)$ & $\approx 7.06$ \\
        \hline
    \end{tabular}
    }
    \caption{Esempio operativo di LogLog}
    \label{tab:loglog-esempio}
\end{table}

Ogni aggiornamento modifica un solo registro, quindi il costo di aggiornamento
resta costante. La differenza rispetto a PCSA è che l'informazione non viene
più distribuita su bitmap, ma su registri che memorizzano un massimo.

\paragraph{Errore}
L'errore relativo standard tipico è dell'ordine
\[
1.30/\sqrt{m}
\]
\cite{Durand_Flajolet_2003}

\paragraph{Complessità}
Se ogni registro codifica valori in $[0,L-p+1]$, servono
$\lceil\log_2(L-p+2)\rceil$ bit per registro. Quindi lo spazio è
\[
S_{\text{LogLog}}(m,L,p)=\Theta\!\bigl(m\log(L-p+2)\bigr)\,\text{bit}
\]
L'aggiornamento è $\Theta(1)$ per elemento e l'interrogazione è $\Theta(m)$.

\paragraph{Limiti pratici}
LogLog riduce molto la varianza rispetto a PCSA, ma resta sensibile alla
costante di normalizzazione e alla qualità dell'hash. Per cardinalità basse
rispetto a $m$, il bias può essere ancora marcato.

La sensibilità residua di LogLog e la necessità di uno stimatore più stabile
portano nel 2007 a HyperLogLog, che conserva i registri ma cambia la regola di
stima.

\subsection{HyperLogLog}
HyperLogLog mantiene la stessa struttura a registri di LogLog ma sostituisce la
media geometrica con una media armonica delle quantità $2^{-M[j]}$
\cite{Flajolet_Fusy_Gandouet_Meunier_2007}

\begin{definition}[HyperLogLog]
Definendo
\[
Z=\sum_{j=0}^{m-1}2^{-M[j]}
\]
la stima grezza di HLL è
\[
E=\alpha_m\frac{m^2}{Z}
\]

Per i valori di $m$ più piccoli si usano costanti pratiche dedicate, mentre per
$m\ge 128$ si usa tipicamente
\[
\alpha_m \approx \frac{0.7213}{1+1.079/m}
\]

Se $V_0$ è il numero di registri a zero, la variante pratica distingue tre
regimi:
\[
\hat{F}_0=
\begin{cases}
	m\log(m/V_0), & \text{se } E\le \frac{5}{2}m \text{ e } V_0>0 \\[2mm]
	E, & \text{nel regime centrale} \\[2mm]
		-2^{L}\log\!\left(1-\frac{E}{2^{L}}\right), & \text{nel regime vicino a }2^{L}
\end{cases}
\]
\end{definition}

La scelta della media armonica attenua il peso dei registri estremi e rende la
stima più stabile di quella ottenuta con LogLog.

\begin{algorithm}[htbp]
    \caption{HyperLogLog}
    \label{alg:hll}
    \begin{algorithmic}
        \STATE Choose the precision $p$ and set $m=2^p$
        \STATE Initialize the registers $M[0],\dots,M[m-1]\leftarrow 0$
        \FOR{each element $x$ in the stream}
            \STATE $y \leftarrow h(x)$
            \STATE $j \leftarrow \textsc{Prefix}_p(y)$
            \STATE $w \leftarrow \textsc{Suffix}_{L-p}(y)$
            \STATE $M[j] \leftarrow \max\{M[j],\rho(w)\}$
        \ENDFOR
        \STATE $Z \leftarrow \sum_{j=0}^{m-1} 2^{-M[j]}$
        \STATE $E \leftarrow \alpha_m m^2 / Z$
        \IF{$E \leq \frac{5}{2}m$}
            \STATE $V_0 \leftarrow$ number of zero registers
            \IF{$V_0 \neq 0$}
                \STATE $E^\star \leftarrow m\log(m/V_0)$
            \ELSE
                \STATE $E^\star \leftarrow E$
            \ENDIF
        \ELSIF{$E \leq \frac{1}{30}\cdot 2^{L}$}
            \STATE $E^\star \leftarrow E$
        \ELSE
            \STATE $E^\star \leftarrow -2^{L}\log(1-E/2^{L})$
        \ENDIF
        \STATE \textbf{return} $\hat{F}_0 \leftarrow E^\star$
    \end{algorithmic}
\end{algorithm}

\paragraph{Esempio}
Supponiamo $m=16$ e che, dopo l'aggiornamento dello sketch, restino
$V_0=10$ registri nulli. In questo caso il conteggio lineare produce
\[
16\log(16/10)\approx 7.52
\]
Finché molti registri restano nulli, questa correzione è più affidabile della
stima grezza $E$. Quando invece $V_0$ diminuisce, il metodo passa al regime
centrale basato sulla media armonica.

\paragraph{Errore}
Il risultato classico è una deviazione standard relativa tipica
\[
1.04/\sqrt{m}
\]
\cite{Flajolet_Fusy_Gandouet_Meunier_2007}

\paragraph{Complessità}
Con $m=2^p$ registri, lo spazio è
\[
S_{\text{HLL}}(m,L,p)=\Theta\!\bigl(m\log(L-p+2)\bigr)\,\text{bit}
\]
In pratica i registri vengono spesso implementati con ampiezza fissa
di 5 o 6 bit. Il tempo di aggiornamento è $\Theta(1)$ per elemento e
l'interrogazione è $\Theta(m)$.

\paragraph{Limiti pratici}
Le formule precedenti assumono parametrizzazione coerente dello sketch, stessa
definizione di rango e funzione di hash sufficientemente uniforme. In presenza
di hash distorto, i registri non campionano più correttamente il flusso e la
stima può mostrare bias non trascurabile.

\paragraph{Correzioni di intervallo e riduzione del bias}
Prima di passare a HyperLogLog++, conviene isolare un punto. HyperLogLog non
coincide con una sola formula, ma con una procedura a regimi. Nei regimi piccoli
si usa il conteggio lineare quando molti registri restano nulli, nel regime
centrale si usa lo stimatore armonico, e vicino al limite del dominio hash si
applica una correzione di saturazione. HyperLogLog++ parte da questa struttura,
ma aggiunge una correzione empirica del bias e una rappresentazione sparsa per
gestire meglio le cardinalità piccole e intermedie.

\subsection{HyperLogLog++}
HyperLogLog++ nasce come evoluzione pratica di HLL in scenari industriali su
larga scala. I miglioramenti principali sono l'uso di hash a 64 bit, la
rappresentazione sparsa nelle cardinalità piccole, la correzione empirica del
bias e la scelta adattiva tra conteggio lineare e stima HLL corretta
\cite{Heule_Nunkesser_Hall_2013}

\begin{definition}[HyperLogLog++]
La struttura mantiene la stessa regola di aggiornamento di HLL sui registri, ma
introduce due passaggi aggiuntivi: rappresentazione sparsa nelle cardinalità
piccole e correzione empirica del bias.

Indicando con $E$ la stima HLL grezza, la stima corretta è
\[
E_{\mathrm{corr}}=
\begin{cases}
E-\beta(E,p), & \text{se } E\le 5m \\[1mm]
E, & \text{altrimenti}
\end{cases}
\]
dove $\beta(E,p)$ è il termine di bias tabulato per la precisione $p$

La scelta finale tra conteggio lineare e stima corretta usa una soglia empirica
\textsc{Threshold}$(p)$ applicata alla stima prodotta nei regimi di cardinalità
bassa

La formulazione del lavoro del 2013 usa inoltre due precisioni: $p$ per la
rappresentazione densa e $p'$ per la codifica sparsa, con $p \le p' \le 64$
\end{definition}

Nel formato sparso lo sketch memorizza soltanto i registri non nulli sotto
forma di coppie indice-valore. Quando questa forma non è più conveniente in
memoria, lo sketch passa alla rappresentazione densa.

\begin{algorithm}[htbp]
    \caption{HyperLogLog++}
    \label{alg:hllpp}
    \begin{algorithmic}
        \STATE Input: stream $S$, precisions $p$ and $p'$ with $p \le p' \le 64$
        \STATE $m \leftarrow 2^p$, \quad $m' \leftarrow 2^{p'}$
        \STATE Initialize sparse representation, temporary set, and dense registers
        \FOR{each element $x$ in $S$}
            \STATE $y \leftarrow h_{64}(x)$
            \IF{the structure is in sparse format}
                \STATE Encode the sparse value associated with $y$
                \STATE Insert the value into the temporary set
                \IF{the temporary set is too large}
                    \STATE Merge the temporary set into the sparse list
                \ENDIF
                \IF{the sparse representation is no longer convenient}
                    \STATE Convert the sketch to dense format
                \ENDIF
            \ELSE
                \STATE Extract $(j,\rho)$ from $y$
                \STATE $M[j] \leftarrow \max\{M[j],\rho\}$
            \ENDIF
        \ENDFOR
        \IF{the structure is in sparse format}
            \STATE $k_{\text{sp}} \leftarrow$ number of nonzero entries in the sparse representation
            \STATE \textbf{return} $\textsc{LinearCounting}(m',m'-k_{\text{sp}})$
        \ELSE
            \STATE Compute the raw estimate $E$
            \IF{$E \le 5m$}
                \STATE $E_{\mathrm{corr}} \leftarrow E-\beta(E,p)$
            \ELSE
                \STATE $E_{\mathrm{corr}} \leftarrow E$
            \ENDIF
            \STATE $V_0 \leftarrow$ number of zero registers
            \IF{$V_0>0$}
                \STATE $H \leftarrow \textsc{LinearCounting}(m,V_0)$
            \ELSE
                \STATE $H \leftarrow E_{\mathrm{corr}}$
            \ENDIF
            \IF{$H \leq \textsc{Threshold}(p)$}
                \STATE \textbf{return} $H$
            \ELSE
                \STATE \textbf{return} $E_{\mathrm{corr}}$
            \ENDIF
        \ENDIF
    \end{algorithmic}
\end{algorithm}

\paragraph{Esempio}
Con $p=14$, quindi $m=16384$ registri, e cardinalità ancora bassa, lo sketch
può restare in formato sparso. Se per esempio nella struttura sono presenti
$k_{\text{sp}}=120$ voci non nulle, lo spazio usato è molto inferiore rispetto
alla rappresentazione densa. Quando $k_{\text{sp}}$ supera la soglia di
conversione, lo sketch passa alla forma densa; da quel momento la stima viene
calcolata correggendo il bias per i valori bassi di $E$ e confrontando il
conteggio lineare con la soglia empirica.

\paragraph{Errore}
HLL++ mantiene l'ordine asintotico tipico della famiglia, cioè
$\Theta(1/\sqrt{m})$, ma riduce in modo sensibile il bias pratico soprattutto
nei regimi piccoli e intermedi. La differenza rispetto a HLL non è nell'ordine
dominante, ma nella qualità empirica della stima e nella gestione dei regimi di
cardinalità.

\paragraph{Complessità}
In modalità sparsa, lo spazio è
\[
\Theta(k_{\text{sp}})
\]
fino alla soglia di conversione. In modalità densa, lo spazio è
\[
\Theta(m)
\]
registri, spesso intorno a $6m$ bit in implementazioni pratiche.

L'aggiornamento è $\Theta(1)$ ammortizzato, con costo $\Theta(m)$ nel momento
della conversione da sparso a denso. L'interrogazione è
$\Theta(k_{\text{sp}})$ in formato sparso e $\Theta(m)$ in formato
denso.

\paragraph{Limiti e compromessi}
HLL++ riduce il bias pratico e gestisce meglio le cardinalità piccole, ma
introduce maggiore complessità ingegneristica: soglie di cambio di regime,
tabelle di correzione e doppia rappresentazione dello stato. Per questa
ragione, nel seguito della tesi sarà importante distinguere sempre la specifica
algoritmica dai dettagli implementativi.

\section{Merge di sketch e scenari distribuiti}
Una proprietà fondamentale degli sketch di cardinalità moderni è la
mergeabilità, cioè la possibilità di costruire sketch locali su partizioni del
flusso e combinarli in uno sketch globale senza rivedere i dati originali
\cite{Agarwal_Cormode_Huang_Phillips_Wei_Yi_2012}

Per Probabilistic Counting e PCSA, dove lo stato è basato su bitmap, la
combinazione naturale è una OR componente per componente. Per LogLog, HLL e
HLL++, mantenendo la notazione del Capitolo~\ref{chp:background}, la regola di
merge naturale è
\[
\bigl(\mathcal{K}(S_1)\oplus \mathcal{K}(S_2)\bigr)[j]
=\max\{\mathcal{K}(S_1)[j],\mathcal{K}(S_2)[j]\}
\]

Per gli sketch a registri vale infatti, per ogni indice $j$,
\[
\mathcal{K}(S)[j]=\max_{x\in S:\,j(x)=j}\rho(w(x))
\]
Ne segue che, a parità di parametrizzazione e semantica di hashing, il merge
registro per registro coincide con lo stato ottenuto processando l'unione dei
flussi.

Nel caso omogeneo, cioè stessa parametrizzazione e stessa semantica di hashing,
questa operazione coincide con lo stato che si otterrebbe processando in modo
sequenziale l'unione dei due flussi. Nel lessico introdotto nel
Capitolo~\ref{chp:background}, questo è un caso semanticamente valido.

Il merge di sketch, però, non si esaurisce nel caso omogeneo. Nella tesi il
punto più importante è che HyperLogLog++ permette di studiare anche casi
eterogenei. Se cambia la precisione ma non cambia la semantica dell'hash, si
ha un caso semanticamente \texttt{recoverable}, in cui la riduzione alla
precisione più bassa rende confrontabili gli sketch. Se invece cambia la
semantica di hashing, il caso è \texttt{invalid}. Questo è il collegamento
diretto con il Capitolo~\ref{chp:implementazione_nuovo}, in cui queste
situazioni vengono rese operative nell'infrastruttura sperimentale.

\section{Confronto sintetico tra gli approcci}
Dopo aver discusso anche i regimi di correzione e la mergeabilità, si può
riassumere la traiettoria storica con due tabelle di confronto.

\begin{table}[htbp]
    \centering
    \small
    \setlength{\tabcolsep}{4pt}
    \renewcommand{\arraystretch}{1.15}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|p{2.5cm}|p{3.0cm}|p{3.0cm}|p{3.6cm}|p{3.2cm}|}
        \hline
        Algoritmo & Stato dello sketch & Regola di stima & Punti di forza & Limiti pratici \\
        \hline
        Probabilistic Counting & Una bitmap singola & Primo zero e costante di calibrazione & Metodo originario basato su bitmap & Varianza elevata, stima poco stabile \\
        \hline
        PCSA & Più bitmap partizionate tramite hash & Mediazione stocastica su bitmap & Riduce la varianza rispetto a una sola bitmap & Stato più pesante e meno compatto delle famiglie a registri \\
        \hline
        LogLog & Array di registri & Media geometrica normalizzata & Riduce la varianza usando registri & Sensibilità alla costante di normalizzazione e al regime di cardinalità bassa \\
        \hline
        HyperLogLog & Array di registri & Media armonica con correzioni di intervallo & Stima più stabile e merge naturale a registri & Richiede gestione distinta dei diversi regimi di cardinalità \\
        \hline
        HyperLogLog++ & Registri con rappresentazione sparsa e densa & HLL corretto dal bias con soglia empirica tra regimi & Aggiunge correzione del bias e rappresentazione sparsa & Maggiore complessità ingegneristica \\
        \hline
    \end{tabular}
    }
    \caption{Confronto qualitativo tra gli algoritmi principali per il conteggio dei distinti}
    \label{tab:confronto-count-distinct}
\end{table}

\begin{table}[htbp]
    \centering
    \scriptsize
    \setlength{\tabcolsep}{3pt}
    \renewcommand{\arraystretch}{1.15}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|p{2.2cm}|p{2.9cm}|p{2.1cm}|p{2.1cm}|p{2.6cm}|}
        \hline
        Algoritmo & Spazio & Aggiornamento & Interrogazione & Errore tipico \\
        \hline
        Probabilistic Counting & $\Theta(L)$ bit & $\Theta(1)$ & $\Theta(L)$ & elevato, poco stabile \\
        \hline
        PCSA & $\Theta(mL)$ bit & $\Theta(1)$ & $\Theta(mL)$ o $\Theta(m)$ & $\approx 0.78/\sqrt{m}$ \\
        \hline
        LogLog & $\Theta(m\log(L-p+2))$ bit & $\Theta(1)$ & $\Theta(m)$ & $\approx 1.30/\sqrt{m}$ \\
        \hline
        HyperLogLog & $\Theta(m\log(L-p+2))$ bit & $\Theta(1)$ & $\Theta(m)$ & $\approx 1.04/\sqrt{m}$ \\
        \hline
        HyperLogLog++ & $\Theta(k_{\text{sp}})$ o $\Theta(m)$ registri & $\Theta(1)$ ammortizzato & $\Theta(k_{\text{sp}})$ o $\Theta(m)$ & $\Theta(1/\sqrt{m})$ con bias pratico ridotto \\
        \hline
    \end{tabular}
    }
    \caption{Confronto di complessità e comportamento dell'errore per gli algoritmi principali}
    \label{tab:confronto-complessita-count-distinct}
\end{table}

Le due tabelle vanno lette insieme. La prima mette a fuoco la forma dello
stato, la seconda mostra come cambiano costo in memoria, tempi operativi ed
errore al cambiare della struttura.

\section{Altri problemi di sketching}
Le due strutture che seguono non appartengono alla genealogia
Probabilistic Counting, PCSA, LogLog, HyperLogLog, HyperLogLog++, ma mostrano
come lo sketching si estenda ad altri problemi. Si parte dal Bloom filter,
nato per la membership approssimata, e si passa poi al Count-Min Sketch, che è
diventato un riferimento classico per la stima di frequenze.

\subsection{Bloom filter}
\begin{definition}[Bloom filter]
Un Bloom filter è un vettore di $m_{\text{bf}}$ bit con $k_{\text{bf}}$
funzioni di hash. L'inserimento di $x$ imposta a uno le celle
$B[h_1(x)],\dots,B[h_{k_{\text{bf}}}(x)]$. Una interrogazione di appartenenza
risponde ``presente'' se tutte queste celle valgono uno.

Assumendo che le posizioni prodotte dalle funzioni di hash siano uniformi e si
comportino come scelte indipendenti, la struttura non produce falsi negativi,
ma può produrre falsi positivi con probabilità approssimativa
\[
p_{\text{fp}}\approx\left(1-e^{-k_{\text{bf}}n_{\text{ins}}/m_{\text{bf}}}\right)^{k_{\text{bf}}}
\]
\cite{Bloom_1970,Agarwal_Trachtenberg_2006}
\end{definition}

\begin{algorithm}[htbp]
    \caption{Bloom filter}
    \label{alg:bloom}
    \begin{algorithmic}
        \STATE Choose $k_{\text{bf}}$ hash functions and a bit vector of length $m_{\text{bf}}$
        \STATE Initialize all bits to zero
        \FOR{each insertion of element $x$}
            \FOR{$j=1$ \TO $k_{\text{bf}}$}
                \STATE Set $B[h_j(x)]\leftarrow 1$
            \ENDFOR
        \ENDFOR
        \FOR{each membership query on $x$}
            \IF{$B[h_j(x)]=1$ for every $j$}
                \STATE \textbf{return} ``present''
            \ELSE
                \STATE \textbf{return} ``absent''
            \ENDIF
        \ENDFOR
    \end{algorithmic}
\end{algorithm}

\paragraph{Esempio}
Consideriamo un Bloom filter con $m_{\text{bf}}=10$ bit e
$k_{\text{bf}}=2$ funzioni di hash. Supponiamo
$h_1(a)=1,\ h_2(a)=6$, $h_1(b)=4,\ h_2(b)=6$, $h_1(c)=1,\ h_2(c)=8$.

\begin{table}[htbp]
    \centering
    \small
    \setlength{\tabcolsep}{4pt}
    \renewcommand{\arraystretch}{1.1}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|c|c|c|c|c|c|}
        \hline
        Passo & Operazione & $(h_1,h_2)$ & Celle toccate & Stato dei bit $[b_0\dots b_9]$ & Esito \\
        \hline
        1 & inserisci($a$) & $(1,6)$ & $b_1,b_6\leftarrow 1$ & $[0100001000]$ & -- \\
        \hline
        2 & inserisci($b$) & $(4,6)$ & $b_4,b_6\leftarrow 1$ & $[0100101000]$ & -- \\
        \hline
        3 & inserisci($c$) & $(1,8)$ & $b_1,b_8\leftarrow 1$ & $[0100101010]$ & -- \\
        \hline
        4 & interroga($z$) & $(1,4)$ & verifica $b_1,b_4$ & $[0100101010]$ & ``presente'' \\
        \hline
        5 & interroga($q$) & $(2,9)$ & verifica $b_2,b_9$ & $[0100101010]$ & ``assente'' \\
        \hline
    \end{tabular}
    }
    \caption{Esempio operativo di Bloom filter}
    \label{tab:bloom-esempio}
\end{table}

L'interrogazione su $z$ produce un falso positivo, mentre l'interrogazione su
$q$ risponde in modo corretto con ``assente''. L'esempio mostra il
comportamento tipico della struttura.

\paragraph{Complessità}
Lo spazio è esattamente
\[
m_{\text{bf}}\,\text{bit}
\]
L'aggiornamento e l'interrogazione costano
\[
\Theta(k_{\text{bf}})
\]
La combinazione di due strutture compatibili avviene tramite OR bit a bit.

\subsection{Count-Min Sketch}
Tra gli sketch per frequenze, il Count-Min Sketch è una delle strutture che si
sono imposte come riferimento classico nella letteratura sui flussi di dati.

\begin{definition}[Count-Min Sketch]
Il Count-Min Sketch mantiene una matrice di contatori
$C\in\mathbb{N}^{d\times w_{\text{cm}}}$ e $d$ funzioni di hash
$h_1,\dots,h_d:\mathcal{U}\to[w_{\text{cm}}]$. Per ogni aggiornamento
dell'elemento $x$, si incrementa $C[j,h_j(x)]$ per ogni riga $j$. La stima
puntuale della frequenza è
\[
\hat f(x)=\min_{j\in[d]} C[j,h_j(x)]
\]

Assumendo funzioni di hash indipendenti tratte da una famiglia 2-universale, e
ponendo
\[
w_{\text{cm}}=\lceil e/\varepsilon\rceil
\qquad
d=\lceil\ln(1/\delta)\rceil
\]
vale con probabilità almeno $1-\delta$
\[
f(x)\le \hat f(x)\le f(x)+\varepsilon\|f\|_1
\]
\cite{Cormode_CountMinSketch_Encyclopedia}
\end{definition}

\begin{algorithm}[htbp]
    \caption{Count-Min Sketch}
    \label{alg:cms}
    \begin{algorithmic}
        \STATE Choose $d$ hash functions and a matrix $C$ of size $d\times w_{\text{cm}}$
        \STATE Initialize all counters to zero
        \FOR{each update of element $x$}
            \FOR{$j=1$ \TO $d$}
                \STATE Increment $C[j,h_j(x)]$
            \ENDFOR
        \ENDFOR
        \FOR{each query on $x$}
            \STATE \textbf{return} $\min_{j\in[d]} C[j,h_j(x)]$
        \ENDFOR
    \end{algorithmic}
\end{algorithm}

\paragraph{Esempio}
Consideriamo un Count-Min Sketch con $d=2$ righe e $w_{\text{cm}}=5$ colonne.
Supponiamo
$h_1(a)=1,\ h_2(a)=3$, $h_1(b)=4,\ h_2(b)=1$, $h_1(c)=1,\ h_2(c)=4$
e processiamo il flusso $a,b,a,c,a$.

\begin{table}[htbp]
    \centering
    \small
    \setlength{\tabcolsep}{4pt}
    \renewcommand{\arraystretch}{1.1}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{|c|c|c|c|c|c|}
        \hline
        Passo & $x$ & $(h_1(x),h_2(x))$ & Aggiornamento & Stato riga 1 & Stato riga 2 \\
        \hline
        1 & $a$ & $(1,3)$ & $C[1,1]++,\ C[2,3]++$ & $[0,1,0,0,0]$ & $[0,0,0,1,0]$ \\
        \hline
        2 & $b$ & $(4,1)$ & $C[1,4]++,\ C[2,1]++$ & $[0,1,0,0,1]$ & $[0,1,0,1,0]$ \\
        \hline
        3 & $a$ & $(1,3)$ & $C[1,1]++,\ C[2,3]++$ & $[0,2,0,0,1]$ & $[0,1,0,2,0]$ \\
        \hline
        4 & $c$ & $(1,4)$ & $C[1,1]++,\ C[2,4]++$ & $[0,3,0,0,1]$ & $[0,1,0,2,1]$ \\
        \hline
        5 & $a$ & $(1,3)$ & $C[1,1]++,\ C[2,3]++$ & $[0,4,0,0,1]$ & $[0,1,0,3,1]$ \\
        \hline
    \end{tabular}
    }
    \caption{Esempio operativo di Count-Min Sketch}
    \label{tab:cms-esempio}
\end{table}

Dallo stato finale si ottiene
\[
\hat f(a)=\min\{4,3\}=3
\]
che in questo caso coincide con il valore esatto, pur restando la struttura in
generale sovrastimante in presenza di collisioni.

\paragraph{Complessità}
Lo spazio è
\[
\Theta(d\,w_{\text{cm}}\log\|f\|_1)\,\text{bit}
\]
se si usano contatori interi standard. Aggiornamento e interrogazione puntuale
costano
\[
\Theta(d)=\Theta(\log(1/\delta))
\]
La combinazione di due strutture compatibili avviene per somma componente per
componente.
